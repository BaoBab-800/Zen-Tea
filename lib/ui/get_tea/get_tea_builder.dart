import 'package:flutter/material.dart';

class GetTeaBuilder extends StatelessWidget {
  const GetTeaBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}